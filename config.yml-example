# Settings for auruby.
# This is a YAML file, which is safer and clearer than a Ruby script.

max_results: 15
number_cores: 4

# Put build options as a single string, just like before.
makepkg_options: '!debug strip'
